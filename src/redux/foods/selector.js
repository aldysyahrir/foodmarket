

export const bannerSelector = (state) => state.foods.banner;

export const newTasteSelector = (state) => state.foods.newTaste;

export const popularSelector = (state) => state.foods.popular;

export const recommendedSelector = (state) => state.foods.recommended;